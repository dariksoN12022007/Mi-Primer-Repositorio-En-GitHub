
//ejercicio del triangulo 

#include<stdio.h>
int main()  {
        
        int lado1 = 9;
        int lado2 = 6;
        int base = 8;
        int altura = 6;
        int area =  (base * altura) / 2;
        int perimetro = lado1 + lado2 + base; 

        printf("El area del triangulo es: %d\n", area);
        printf("El perimtero del triangulo es: %d\n", perimetro);
return 0;
}

